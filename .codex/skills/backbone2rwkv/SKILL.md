---
name: backbone-to-rwkv7
description: 将项目的骨干网络替换为RWKV-7架构，以取得比Mamba、Gated-DeltaNet等架构更好的效果。
---

Backbone to RWKV-7用于将项目的骨干网络替换为RWKV-7架构。
严格按照顺序执行以下步骤，在前三步执行完毕前，先不要探索项目结构。
当任何一步出现问题时，立即停止并报告问题，而非继续执行后续步骤。

# 1. 告知用户注意事项

输出以下注意事项给用户，不要遗漏：

- 警告用户必须阅读此注意事项。
- 简要描述整个过程的大致流程（我将通过以下流程...）。
- 告知用户需要手动配制好用于支持RWKV-7运行的环境（依赖于Pytorch和CUDA），而你会进行必要的检查。

# 2. 询问基本信息

- 除非你已经清楚，否则询问清楚项目具体运行环境（例如名为example的conda虚拟环境）和程序的具体入口（例如`python train.py`），不要出选择题，而是直接让用户输入。

# 3. 确认运行环境

运行环境验证方法：skill自带`scripts/test_rwkv7.py`脚本。你必须在在用户的环境中，直接运行`scripts/test_rwkv7.py`脚本，并检查输出结果以验证RWKV-7的运行环境是否配置正确，而非只检查Pytorch和CUDA状态。
如果环境配置正确，继续下一步；否则，提示用户修正环境配置问题，然后退出流程。

# 4. 识别现有骨干网络，并规划替换策略

## 4.1 前置知识（RWKV-7的核心模块描述）：
```
Time-Mix (RWKV_Tmix_x070)
作用：时序混合模块，可用于替换 Transformer 中的 Self-Attention。捕捉 token 之间的时序依赖关系。

输入: x [B, T, C], v_first [B, T, C]
输出: x [B, T, C], v_first [B, T, C]
B: batch size
T: 序列长度
C: 隐藏维度 (n_embd)

Channel-Mix (RWKV_CMix_x070)
作用：通道混合模块，类似于 Transformer 中的 FFN (Feed-Forward Network)。在特征维度上进行非线性变换，增强模型的表达能力。

输入: x [B, T, C]
输出: x [B, T, C]
```

## 4.2 分析和规划

严格按照以下步骤进行：
- 首先从用户提供的项目运行方法入口中，逐步追溯当前代码使用的骨干网络模块，分析现有骨干网络的工作原理。
- 然后阅读skill下自带的`references/rwkv7_channel_mix.py`和`references/rwkv7_time_mix.py`中的RWKV-7模块实现，结合这些信息，规划将骨干网络替换为RWKV-7的策略。规划替换策略时，首先识别当前项目中骨干网络中建模时序依赖关系的模块和建模特征维度关系的模块分别是什么。例如对于Transformer架构，建模时序依赖关系的模块是Self-Attention，而建模特征维度关系的模块通常是FFN；对于Mamba架构，Mamba模块同时建模时序依赖关系和特征维度关系。除非用户明确指定，否则默认首选替换策略尽可能只使用RWKV-7的Time-Mix模块替换当前项目中建模时序依赖关系的模块，不替换建模特征维度关系的模块。例如对于Transformer，首选替换策略应当是只使用RWKV-7的Time-Mix模块替换Transformer中的Self-Attention模块，不替换FFN模块；对于Mamba架构，由于Mamba模块同时建模时序依赖关系和特征维度关系，因此无法只替换时序建模模块，因此推荐同时使用RWKV-7的Time-Mix和Channel-Mix模块替换Mamba模块。其他架构则具体分析。

## 4.3 输出替换方案报告

进入Plan模式，输出一份报告，包括以下内容：

- 当前项目的模型建模的数据类型（如文本、图像、音频，双向/单向等）。
- 当前项目中使用的骨干网络架构（如Transformer、LSTM、CNN等）。
- 分析当前项目是否可以直接替换为RWKV-7架构，或者需要进行哪些调整。
- 如果当前项目骨干网络可以替换为RWKV-7架构，则针对当前项目的任务类型（如分类、生成、回归等），推荐RWKV-7替换策略。
- 分析参数配平方案，使得替换后的RWKV-7模型参数量与当前骨干网络的参数量尽可能接近。
- 注意除了骨干网络之外，其他部分（如扫描顺序等）应尽量保持原始代码的逻辑

输出报告后，询问用户是否同意该替换方案。如果用户同意，继续下一步；否则，与用户讨论并调整替换方案，直到达成一致。

# 5. 替换为RWKV-7架构

- 创建一个新的分支，如`convert-to-rwkv7`，然后切换到该分支。
- 根据替换方案，生成代码以替换当前项目中的骨干网络为RWKV-7架构，替换时，直接参考`references/rwkv7_channel_mix.py`和`references/rwkv7_time_mix.py`中的实现，尽可能多地复用这些代码。
- 根据用户给出的项目运行方法，给出完成替换后，运行RWKV-7版本项目的命令或脚本。